# DancenotationMCP
Build a local, single-user, headless-first dance notation MCP server inspired by LabanWriter.  The product goal is to let an AI agent generate professional dance notation from natural-language prompts, validate it, repair it if needed, and render it to SVG.  This is not a GUI clone. It is an agent-native notation runtime.
